// SIP-Creator Mapping file

// ## RecordRoot record 215

// ConstantField collectionId 92001
// ConstantField europeana_provider The European Library
// ConstantField europeana_dataProvider The European Library
// ConstantField europeana_language mul
// ConstantField europeana_collectionTitle Tel Treasures
// ConstantField europeana_collectionName 92001_Ag_EU_TELtreasures
// ConstantField europeana_country europe
// ConstantField europeana_rights unknown

/* ValueMap */ def icn_acquisitionMeansMap = [ // aankoop,bruikleen,legaat,opdracht,overdracht,ruil,schenking,successieregeling,
'aankoop':'',
'legaat':'',
'overdracht':'',
'ruil':'',
'schenking':'',
'successieregeling':'',
]
def icn_acquisitionMeans = { def v = icn_acquisitionMeansMap[it.toString()]; return v ? v : it }
/* ValueMap */ def icn_techniqueMap = [ //  dvd, video,anders,film,fotografie,schilderkunst,textiel,werk op papier,
'film, video, dvd':'',
'fotografie':'',
'schilderkunst':'',
'textiel':'',
'werk op papier':'',
]
def icn_technique = { def v = icn_techniqueMap[it.toString()]; return v ? v : it }
output.record {

//<<<europeana_collectionName
europeana.collectionName europeana_collectionName
//>>>

//<<<europeana_collectionId
europeana.collectionTitle europeana_collectionTitle
//>>>

//<<<europeana_language
europeana.language europeana_language
//>>>

//<<<europeana_provider
europeana.provider europeana_provider
//>>>

//<<<dc_creator
input.dc_creator.each {
dc.creator it
}
//>>>

//<<<dc_date
input.dc_date.each {
dc.date extractYear(it)[0]
}
//>>>

//<<<dc_description
input.dc_description.each {
dc.description it
}
//>>>

//<<<dc_subject
input.dc_subject.each {
dc.subject it
}
//>>>

//<<<dc_title
input.dc_title.each {
dc.title it
}
//>>>

//<<<dc_type
input.dc_type.each {
dc.type it
}
//>>>

//<<<dcterms_alternative
input.dcterms_alternative.each {
dcterms.alternative it
}
//>>>

//<<<dcterms_extent
input.dcterms_extent.each {
dcterms.extent it
}
//>>>

//<<<europeana_isShownBy
input.europeana_isShownBy.each {
europeana.isShownBy it
}
//>>>

//<<<europeana_object
input.europeana_object.each {
europeana.object it
}
//>>>

//<<<europeana_uri
europeana.uri createEuropeanaURI(input.europeana_uri[0])
//>>>

//<<<europeana_year
input.europeana_year.each {
for (part in extractYear(it)) {
europeana.year part
}
}
//>>>

//<<<europeana_type
europeana.type 'IMAGE'
//>>>

//<<<europeana_country
europeana.country 'europe'
//>>>

//<<<europeana_rights
europeana.rights 'unknown'
//>>>

//<<<europeana_dataProvider
europeana.dataProvider 'The Europeana Library'
//>>>

//<<<icn_acquiredWithHelpFrom
input.icn_acquiredWithHelpFrom.each {
icn.acquiredWithHelpFrom it
}
//>>>

//<<<icn_acquisitionMeans
input.icn_acquisitionMeans.each {
icn.acquisitionMeans it
}
//>>>

//<<<icn_acquisitionYear
input.icn.acquisitionYear.each { extractYear(it)}
//>>>

//<<<icn_collectionPart
input.icn_collectionPart.each {
icn.collectionPart it
}
//>>>

//<<<icn_creatorYearOfBirth
input.icn_creatorYearOfBirth.each {extractYear(it)}
//>>>

//<<<icn_location
input.icn_location.each {
icn.location it
}
//>>>

//<<<icn_material
input.icn_material.each {
icn.material it
}
//>>>

//<<<icn_physicalState
input.icn_physicalState.each {
icn.physicalState it
}
//>>>

//<<<icn_province
input.icn_province.each {
icn.province it
}
//>>>

//<<<icn_purchasePrice
input.icn_purchasePrice.each { icn.purchasePrice it }
//>>>

//<<<icn_technique
input.icn_technique.each {
icn.technique it
}
//>>>
}
