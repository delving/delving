<?xml version="1.0" encoding="UTF-8"?>
<beans
        xmlns="http://www.springframework.org/schema/beans"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:util="http://www.springframework.org/schema/util"
        xmlns:security="http://www.springframework.org/schema/security"
        xmlns:context="http://www.springframework.org/schema/context"
        xmlns:tx="http://www.springframework.org/schema/tx"
        xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
        http://www.springframework.org/schema/util
        http://www.springframework.org/schema/util/spring-util-3.0.xsd
        http://www.springframework.org/schema/security
        http://www.springframework.org/schema/security/spring-security-3.0.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-2.5.xsd">

    <bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="properties" ref="launchProperties"/>
    </bean>

    <context:annotation-config/>

    <tx:annotation-driven proxy-target-class="true"/>

    <bean id="mongo" class="com.mongodb.Mongo"/>

    <bean id="metaRepoConfig" class="eu.delving.services.impl.MetaConfigImpl"/>

    <bean id="metadataRepository" class="eu.delving.services.impl.MetaRepoImpl">
        <property name="mongo" ref="mongo"/>
        <property name="annotationProcessor" ref="annotationProcessor"/>
        <property name="metaRepoConfig" ref="metaRepoConfig"/>
        <property name="responseListSize" value="#{launchProperties['responseListSize']}"/>
        <property name="harvestStepSecondsToLive" value="180"/>
    </bean>

    <bean id="cleanupTrigger" class="org.springframework.scheduling.quartz.SimpleTriggerBean">
        <property name="jobDetail">
            <bean class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
                <property name="targetObject" ref="metadataRepository"/>
                <property name="targetMethod" value="removeExpiredHarvestSteps"/>
                <property name="concurrent" value="false"/>
            </bean>
        </property>
        <property name="startDelay" value="#{launchProperties['harvestStepCleanupDelay']}"/>
        <property name="repeatInterval" value="#{launchProperties['harvestStepCleanupDelay']}"/>
    </bean>

     <bean id="indexJobRunner" class="eu.delving.services.util.IndexJobRunner">
        <property name="consoleDao" ref="consoleDao"/>
        <property name="eseImporter" ref="normalizedEseImporter"/>
    </bean>

    <bean id="indexTrigger" class="org.springframework.scheduling.quartz.SimpleTriggerBean">
        <property name="jobDetail">
            <bean class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
                <property name="targetObject" ref="indexJobRunner"/>
                <property name="targetMethod" value="runParallel"/>
                <property name="concurrent" value="false"/>
            </bean>
        </property>
        <property name="startDelay" value="${trigger.startDelay}"/>
        <property name="repeatInterval" value="${trigger.repeatInterval}"/>
    </bean>

    <bean class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
        <property name="triggers">
            <list>
                <ref bean="indexTrigger"/>
            </list>
        </property>
    </bean>

    <bean class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
        <property name="triggers">
            <list>
                <ref bean="cleanupTrigger"/>
            </list>
        </property>
    </bean>

</beans>